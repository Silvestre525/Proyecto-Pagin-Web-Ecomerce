<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenLigth</title>

    <!-- Google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href='https://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet'>
    <!--CSS  -->
    <link rel="stylesheet" href="/css/register.css">
    <link rel="Stylesheet" href="/css/header-footer.css" type="text/css">
    <!-- Font awesome -->
    <script src="https://kit.fontawesome.com/79b8daf391.js" crossorigin="anonymous"></script>

</head>

<body>


    <main>
        <section class="form-register">
            <form method="POST" action="/register" enctype="multipart/form-data">
                <input class="controls <%= locals.errors && errors.nombre ? 'is-invalid' :null  %> " type="text" name="nombre" id="nombre" 
                placeholder="Ingrese su nombre" value="<%= locals.oldData ? oldData.nombre : null  %> ">
                <% if(locals.errors && errors.nombre){%>
                    <div class="text-danger">
                        <%= errors.nombre.msg  %> 
                    </div>
                    <% } %> 
                <input class="controls <%= locals.errors && errors.apellido ? 'is-invalid' :null  %> " type="text" name="apellido" id="apellido" placeholder="Ingrese su apellido" value="<%= locals.oldData ? oldData.apellido : null  %> ">
                <% if(locals.errors && errors.apellido){%>
                    <div class="text-danger">
                        <%= errors.apellido.msg  %> 
                    </div>
                    <% } %> 
                <input class="controls <%= locals.errors && errors.email ? 'is-invalid' :null  %>" type="text" name="email" id="email" placeholder="Ingrese su email">
                <% if(locals.errors && errors.email){%>
                    <div class="text-danger">
                        <%= errors.email.msg  %> 
                    </div>
                    <% } %> 
                <input class="controls  <%= locals.errors && errors.password? 'is-invalid' :null%>" type="password" name="password" id="password" placeholder="Ingrese su contraseña">
                <% if(locals.errors && errors.password){%>
                    <div class="text-danger">
                        <%= errors.password.msg  %> 
                    </div>
                    <% } %> 
                <label><b>Imagen de perfil:</b></label>
                <input type="file" name="avatar">
                <% if(locals.errors && errors.avatar){%>
                    <div class="text-danger">
                        <%= errors.avatar.msg  %> 
                    </div>
                    <% } %> 
                <p class="terminos">Estoy de acuerdo con <a href="#">Terminos y Condiciones</a></p>
                <button>Registrarse</button>
                <p class="yatengocuenta"><a href="/login">Ya tengo cuenta?</a></p>
            </form>
        </section>

    </main>



</body>

</html>
<script src="/js/validacionRegisto.js"></script>